const displayFilter = document.querySelectorAll('.display_filter')
const years = document.querySelector('#years')
const chartRows = document.querySelectorAll(".chart_row")
const chartBody = document.querySelector(".chart_body")

for (i = 0; i < displayFilter.length; i++) {
    displayFilter[i].addEventListener("click", (event) => {

    })
}

